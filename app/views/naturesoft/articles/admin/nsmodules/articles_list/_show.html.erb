<%
  options = nsmodule.get_options
  category = Naturesoft::Articles::Category.find(options["category_id"])
%>

<%= render "naturesoft/"+nsmodule.engine_name+"/admin/nsmodules/"+nsmodule.module_name+"/templates/" + options["style"],
  nsmodule: nsmodule,
  category: category,
  options: options
%>